# 미니맵과 월드 환경 추가

## 후속 수정: 환경 v5 / 메시 캐시 v2

아래 v4 설명은 최초 구현 기록이다. 현재 동작은 다음과 같이 변경했다.

- 기존 v2~v4 청크는 다음 실행에서 읽힐 때 v5 환경으로 갱신한다. 아직 읽지 않은 파일은 그대로 남고 나중에 읽을 때 갱신된다.
- 시작 거점 주변에 오두막(-8,-8), 감시탑(8,-8), 예배당(-8,8), 묘비(8,8), 우물(6,5) 후보를 배치한다. 길/캠프와 충돌하는 후보는 생략한다.
- 원본 청크는 해당 SaveData 월드의 Backups/before_environment_v5에 파일별로 백업한다. 백업에 실패하면 원본을 덮어쓰지 않는다. migration.log에는 좌표, 버전, 나무 수 변경을 기록한다.
- 월드 시드, 플레이어 성장/아이템/진행도 파일과 영혼 NPC 데이터는 초기화하지 않는다. 기존 봉화의 위치와 점등 상태를 보존한다.
- 환경 변경으로 저장된 플레이어 위치가 막히면 기존 이동 불가 위치 복구 로직이 거점으로 이동시킨다.
- 큰 건물은 2.2배, 우물/묘비는 1.3배로 확대했다. 큰 건물은 중심으로부터 X/Y 각각 1.15타일 범위의 충돌을 사용하며 주변 3×3 영역이 길/캠프를 침범하지 않는 위치에만 새로 배치한다.
- 큰 건물에 외벽 줄눈/프레임, 입구 계단, 따뜻한 등불을 추가했다. 큰 외관을 위해 화면 밖 오브젝트 판정 여유도 늘렸다.
- 형상 변경 때문에 디스크 메시 캐시 버전을 2로 올렸다. 오래된 캐시 파일은 삭제하지 않고 새 폴더를 사용한다.
- 미니맵 중심은 실수 좌표로 지수 보간한다. 타일 정수 좌표는 조회에만 쓰고 표시 위치에는 플레이어 추적 중심의 소수 부분을 반영한다.
- 미니맵에 새로 로딩된 청크는 0.35초에 걸쳐 나타난다. 가장자리 타일은 지도 사각형 안으로 잘라 표시한다. 큰 순간 이동은 오래된 위치를 따라가지 않고 즉시 중심을 재설정한다.
- 미니맵 타일 조회 범위는 보간 여유를 포함해 최대 43×43이다. 별도의 미니맵용 월드 로딩이나 위치별 메시 파일은 만들지 않는다.

복구가 필요하면 게임 종료 후 원본을 별도로 보관하고 백업 청크를 복원할 수 있다. 단, 현재 코드로 다시 실행하면 v5 갱신이 재적용되므로 구버전으로 완전히 돌아가려면 코드 버전도 함께 되돌려야 한다.

이번 작업에서는 저장 파일을 직접 변경하거나 게임을 실행하지 않았다. 백업과 저장 갱신은 사용자가 수정된 게임을 실행할 때 수행된다. 실제 빌드/이동/표시 확인은 미수행이다.

## 미니맵

기존 STREAM MAP 청크 격자를 플레이어 주변 41×41 타일 지도로 교체했다.
상단이 월드 -Y 방향인 고정 방향 지도이며, 플레이어가 타일을 이동할 때 중심도 이동한다.

- 지형: 풀/길/돌/물, 나무, 건물, 폐허, 봉화 표시
- 흰색: 플레이어 / 청록: 주민 / 파랑: 영혼 NPC
- 빨강: 일반 적 / 자홍: 보스 / 금색: 캠프
- 미로딩 지역: 어두운 색. 미니맵 때문에 추가 청크를 로딩하지 않는다.
- 현재 로딩된 주변을 보여주는 지도이며 영구 탐험 지도나 전체 월드 지도는 아니다.
- 600×400 미만 창에서는 기존 HUD와 겹치지 않도록 미니맵을 숨긴다.

## 환경 생성 v4

숲 후보를 12종 패치 중 7종으로 늘리고 기존 수분 조건을 제거했다.
숲/호수 패치 반경 후보를 1~3에서 2~4로 늘리되, 기존 패치 가장자리의 보행 여백 제한은 유지한다.
숲 안의 바위 비율도 1/11에서 1/19로 낮춰 나무가 더 많이 보이게 했다.
호수 패치 비율은 3/12에서 1/12로 낮췄다. 실제 화면 밀도는 시드·길·캠프와 패치 크기에 따라 달라진다.

추가 구조물: 오두막, 예배당, 감시탑, 우물, 묘비 군락.
12종 패치 중 2종은 열린 돌마당과 구조물 하나를 생성한다. 구조물 종류는 시드 기반으로 결정한다.
새 구조물은 현재 외관과 충돌을 가진 랜드마크이며 내부 입장/상점/상호작용 기능은 아직 없다.

구조물은 한 타일의 바닥 면적에 맞춘다. 길과 캠프에는 추가 장애물을 배치하지 않는다.
채워진 숲 패치의 바깥쪽에 보행 여백을 남기고 건물을 둘러싸는 울타리는 만들지 않는다.
이는 생성 규칙 수준의 이동 안전 장치이며 실제 이동 검증은 아직 하지 않았다.

## 저장 호환성

기존 Prop 값 0~4는 유지하고 새 값 5~9를 뒤에 추가했다.
새 청크만 생성 버전 4로 저장하고 기존 버전 2/3 파일은 그대로 읽는다.
따라서 이미 저장된 지역의 나무/구조물은 바뀌지 않는다. 새 환경은 아직 생성되지 않은 지역에서 확인할 수 있다.
기존 월드나 진행도를 삭제하거나 재생성하지 않았다.

## 렌더링과 성능 분석

WorldView.h/.cpp로 미니맵과 새 구조물 외관을 분리했다.
구조물은 기존 PropActor → 타입/variation 메시 캐시 → 배칭/인스턴싱 경로를 사용한다.
기존 가림 반투명 효과와 충돌 판정도 적용된다.
새 Prop 번호는 기존 메시 캐시와 키가 겹치지 않으며 지형 키에는 생성 버전과 타일 서명이 반영된다.

미니맵은 기존 HUD Actor에서 그리며 타일별로 Actor나 파일 캐시를 만들지 않는다.
한 프레임에 최대 1,681개 주변 타일을 조회하고 타일 면은 단위 삼각형 인스턴스로 제출한다.
지도 이동 위치를 메시 캐시 키로 사용하지 않아 이동에 따라 캐시 파일이 계속 늘어나는 것을 피한다.

기존 Logs/render-*.csv에서 ui_draws, instances, upload_bytes, submit_ms를 비교하면 미니맵 추가 비용을 볼 수 있다.
나무가 늘어나는 새 지역에서는 actors, mesh_count, world_draws도 함께 비교한다.
추가된 구조물의 build/disk_hit 이벤트는 prop:5부터 prop:9까지의 키로 구분할 수 있다.
이 문서는 구현 변경 기록이며 성능 향상/비용의 실측 결과는 아니다.

## 사용자 확인 항목

빌드·실행·테스트·렌더링 결과 확인은 수행하지 않았다.

1. 기존 세이브 로드 및 기존 지역 유지 여부
2. 미니맵 이동/지형/길/주민/적/보스/영혼 표시
3. 아직 생성되지 않은 지역의 나무와 새 구조물
4. 길과 패치 사이 이동, 구조물 뒤 반투명 효과
5. 재실행 후 새 청크 로드 및 구조물 파일 캐시 적중
6. 창 크기 변경 시 미니맵과 하단 HUD 배치
