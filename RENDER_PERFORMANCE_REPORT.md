# 렌더링 성능 개선 리포트 — 2026-09-22

## 1. 기준 자료와 결론

사용자가 제공한 4개 구간의 측정값:
- FPS: 61.1 / 61.7 / 62.1 / 61.3
- 프레임당 draw call: 모든 구간 174, 최소/최대도 174

174회라는 수치만으로 GPU 병목이나 성능 부족을 확정할 수 없다. 현재 SimpleGame.cpp의 Tick은 glutTimerFunc(16, Tick, 0)로 재예약된다. 약 62 FPS와 근접하므로 타이머 주기와 화면 교체 대기를 함께 봐야 한다. 이번 변경은 타이머, VSync, 후처리 품질을 바꾸지 않는다.

코드에서 확인한 주요 개선 지점은 DrawMesh가 메시마다 Flush와 glDrawArrays를 실행하던 구조다. 메모리에 캐시된 메시도 매번 별도 호출이었다. 이 경로를 순서 보존 배치 + 연속 동일 메시 인스턴싱으로 변경했다.

변경 후의 실측 FPS, 드로우 콜 수, CPU/GPU 개선율은 아직 없다. 요청에 따라 빌드, 실행, 테스트, 렌더링 확인은 수행하지 않았다.

## 2. 적용 방식

### 서로 다른 메시: 순서 보존 배칭

기존: 동적 정점 Flush → 메시 하나 Draw → 다음 메시에서 반복.
변경: 메시의 로컬 정점을 위치·스케일·불투명도로 변환하여 공용 동적 정점 배치에 추가 → 상태/레이어 경계에서 묶어 Draw.

서로 다른 메시라도 같은 셰이더·텍스처·블렌딩 상태라면 합쳐 그린다. CPU에서 정점을 복사/변환하고 GPU에 업로드하는 비용은 남는다. 드로우 콜 감소와 업로드 감소는 서로 다른 목표다.

배치 기준은 262,144 정점이다. 추가 메시 때문에 기준을 넘으면 먼저 제출한다. 하나의 큰 메시 자체가 기준보다 클 수 있어 엄격한 메모리 상한은 아니다.

### 같은 메시: GPU 인스턴싱

같은 메시가 표시 순서상 연속 4개 이상이면(6정점 이하의 작은 기본 도형은 32개 이상):
1. 해당 메시를 GPU 정적 VBO/VAO에 최초 한 번 올린다.
2. 인스턴스마다 위치, 2×2 선형 변환(스케일/기울기), 색, 알파, 발광 배율만 전달한다.
3. glDrawArraysInstanced 한 번으로 그린다.

최대 4,096 인스턴스 단위로 제출한다. 기준 미만의 짧은 묶음은 별도 드로우를 늘리지 않도록 공용 정점 배치에 합친다. 작은 도형에 더 큰 기준을 사용하는 이유는 적마다 체력바의 삼각형 4개가 전체 배치를 끊지 않도록 하기 위해서다. 기준 4/32는 초기 휴리스틱이며 측정 결과에 따라 조정할 수 있다.

적마다 체력바가 끼어 있거나 메시 종류가 번갈아 나오면 GPU 인스턴싱 비율은 낮아질 수 있다. 이 경우에도 서로 다른 메시를 묶는 배칭은 적용된다. 모든 오브젝트가 GPU 인스턴싱되는 구조는 아니다.

### 표시 순서를 바꾸지 않는 이유

현재 렌더러는 깊이 테스트 없이 알파 블렌딩과 쿼터뷰 정렬에 의존한다. 같은 메시를 화면 전체에서 모아 재정렬하면 나무 뒤 플레이어, 그림자, 영혼, 반투명 효과가 잘못 겹칠 수 있다.

따라서 기존 Actor 정렬을 유지하고 인접한 항목만 묶는다. Ground / GroundOverlay / World / Effects / UI 경계와 후처리 경계에서 제출하며, 한글 텍스처 전환도 배치 경계다. 패스별 호출 수를 정확히 기록하기 위해 레이어 경계를 명시했다.

### 후처리 호출은 별도

PostProcessor의 bloom과 edge blur는 이전 패스 결과를 다음 패스에서 읽는다. 일반 월드 메시처럼 한 draw로 합칠 수 없다. 현재 설정에서는 bloom 7패스, edge blur 5패스, 최종 합성 1패스로 총 13회다. 두 효과를 끄거나 후처리를 끄면 달라진다. 이번 작업에서는 품질과 패스 수를 유지한다.

## 3. 메시 생성과 파일 캐시

기존에도 지형, 구조물, 적, 아이템, 영혼은 BeginMesh/EndMesh 메모리 캐시를 사용하고 있었다. 화면에 보이는 모든 메시를 매 프레임 새로 만드는 구조는 아니었다.

이번에는 이 공통 경로에 파일 캐시를 추가했다:
메모리 조회 → 파일 읽기/검증 → 실패 시 생성 → 파일 저장 → 메모리 재사용.

추가로 단위 삼각형, 20분할 타원, ASCII 글자 도형을 파일/메모리 캐시한다. 타원의 크기·위치·색과 문자의 위치·크기·색은 인스턴스 데이터로 적용한다. 임의의 삼각형도 단위 삼각형의 2×2 변환과 평행이동으로 표현한다. Rect/Quad/Line이 호출하는 Triangle도 이 경로를 이용하므로 체력바, 망토, 선 등의 변화마다 새 정적 메시를 만들지 않는다.

직접 정점 생성은 캐시 미스 시의 메시 기록 과정에 남는다. 캐시가 준비된 이후에는 Triangle 호출마다 형상을 파일로 저장하는 대신 변환 인스턴스만 제출한다.

파일 위치:
실행 파일 폴더/SaveData/<월드 모드>/MeshCache/<월드 시드>/geometry-v1/*.mesh

파일에는 로컬 정점 데이터, 포맷 버전, 정점 크기, 전체 캐시 키, 정점 수, 체크섬을 보관한다. GPU 핸들은 저장하지 않는다. 재실행 시 CPU 메시를 복원하고 실제 인스턴싱이 필요할 때 GPU에 올린다.

보호 및 무효화:
- 전체 키 검증으로 파일명 해시 충돌 시 잘못된 메시를 사용하지 않는다.
- 파일 크기/정점 수/삼각형 개수/체크섬/유한 실수 여부를 검사한다.
- 임시 파일 작성 후 교체한다. 저장 실패 시 메모리 캐시는 계속 사용한다.
- 월드 시드별 폴더를 분리한다.
- 지형 키에는 청크 좌표·생성 버전·ground/prop/variation 서명을 포함한다.
- 형상 생성 코드나 정점 레이아웃을 바꾸면 SceneMeshCache.cpp의 CacheVersion을 올려야 한다.
- 청크가 빠지면 해당 메모리/GPU 캐시는 해제하고 파일은 남긴다.
- 캐시는 파생 데이터이며 원본 월드 저장 데이터와 분리되어 있다.

남아 있는 동적 작업:
플레이어/NPC 애니메이션, 진행률에 따른 체력바, 선과 범위 링 등의 위치/방향/변환 계산은 매 프레임 남는다. 짧은 인스턴스 묶음은 공용 배치로 정점을 복사한다. 무한히 바뀌는 시간·체력·각도를 파일 키로 만들지 않는다. 한글은 기존 텍스처 메모리 캐시와 텍스처 쿼드 제출을 유지하며 이번 메시 파일 캐시 대상이 아니다.

파일 캐시 적중이 곧 GPU 업로드 0을 의미하지 않는다. 혼합 배치는 매 프레임 정점을 업로드한다. 이 비용이 크면 차기 단계로 공용 정적 지오메트리 저장소와 간접 드로우 등을 검토해야 한다.

## 4. 분석 로그

실행 파일 폴더/Logs/에 세션마다 별도 파일을 생성한다:
- render-<세션시각>.csv: 약 1초 구간별 집계
- render-<세션시각>-events.csv: 메시별 캐시 수명 이벤트

콘솔은 FPS/드로우 콜과 주요 비용 요약을 1초마다 출력한다. 매 프레임 집계하되 매 프레임 콘솔 출력은 하지 않는다.

집계 CSV의 각 지표는 last / avg / max 열을 가진다. avg와 max는 해당 행의 프레임 구간에 대한 값이다.

| 지표 | 의미 |
| --- | --- |
| draws | 실제 glDrawArrays + glDrawArraysInstanced 호출 수. 인스턴스 수와 구분 |
| ground/overlay/world/effects/post/ui_draws | 호출이 실제 제출된 패스별 수 |
| instanced_draws / instances | GPU 인스턴싱 호출과 처리 인스턴스 수 |
| submitted_vertices | 인스턴스 반복을 포함한 제출 정점 수 |
| upload_bytes | SceneRenderer 메시/배치/인스턴스 버퍼 업로드 바이트. 텍스처/FBO 제외 |
| generated_vertices | Triangle의 메시 기록 경로에서 새로 생성한 정점 수. 캐시 복사분과 한글 텍스처 쿼드 제외 |
| batch_vertices | 캐시 메시에서 공용 배치로 복사한 정점 수 |
| mesh_requests / memory_hits / disk_hits / cache_misses | BeginMesh 조회 결과 |
| mesh_builds / mesh_build_ms | 실제 형상 생성 건수와 CPU 시간 |
| disk_read/write_bytes / cache_io_ms / cache_errors | 파일 캐시 입출력과 검증/저장 오류 |
| evictions | 스트리밍으로 해제한 지형 메시 수 |
| mesh_count / mesh_cpu_bytes / mesh_gpu_bytes | 캐시 메시 수, CPU capacity, GPU 정적 메시 바이트 추정치 |
| sync_ms / submit_ms / swap_ms / render_ms | 씬 동기화, 렌더 제출, 화면 교체, 전체 렌더 콜백 CPU 경과 시간 |
| actors / chunks / pending_chunks | 부하와 스트리밍 상태 |
| width / height / zoom / 효과 설정 | 측정 환경과 화면 조건 |

render_ms에는 sync/submit/swap 시간이 포함된다. cache_io_ms와 mesh_build_ms는 submit_ms의 일부이므로 합산하면 중복된다. render_ms에는 다음 게임 Tick 처리나 CSV 출력 비용이 들어가지 않는다. 프레임 간격과 FPS에는 이러한 대기·콜백·로그 비용도 영향을 줄 수 있다.

frame_ms_avg는 구간 시간/완료 프레임 수이고 frame_ms_max는 완료 프레임 사이 최대 간격이다. GPU timer query는 아직 없으므로 submit_ms를 GPU 렌더링 시간이라고 해석하면 안 된다. mesh_gpu_bytes도 전체 VRAM 사용량이 아니다.

CSV 맨 앞의 # 행에는 스키마, GPU/드라이버 식별 문자열, 빌드 날짜, Debug/Release, 월드 시드와 모드를 기록한다. CSV 분석 시 # 행을 제외한다.

이벤트 종류:
miss, build, disk_hit, disk_write, invalid, write_failed, evict.
키별 정점 수/바이트와 시각으로 어떤 청크나 형상이 반복 생성되는지 추적한다. 메모리 적중은 매번 이벤트로 쓰지 않고 집계로만 기록한다.

로그와 캐시는 Git에서 제외한다. 세션 파일은 자동 삭제/회전하지 않으므로 장기간 측정 시 용량을 관리해야 한다. 디스크 입출력과 이벤트 기록은 현재 렌더 스레드에서 수행하며 콜드 캐시 지연의 원인이 될 수 있다.

## 5. 실행 후 비교 절차

1. 같은 빌드 구성(Debug/Release), 월드 시드, 위치, 해상도, 줌, 후처리 설정을 고정한다.
2. 최초 실행의 콜드 캐시 구간과 정지 후 안정 구간을 나누어 기록한다.
3. 종료 후 같은 위치에서 재실행한다. disk_hit가 발생하고 불필요한 build가 줄어드는지 확인한다.
4. 같은 경로로 청크 경계를 왕복한다. evict 후 disk_hit로 복원되는지 확인한다.
5. 전투/아이템 다수/한글 대화/반투명 구조물 장면을 별도로 기록한다.
6. 동일 장면에서 draws_avg와 패스별 호출 수, upload_bytes_avg, submit_ms_avg, frame_ms_max를 함께 비교한다.
7. 최대 프레임 지연이 큰 구간은 events.csv와 cache_io_ms, pending_chunks를 대조한다.
8. 외형, 알파 겹침, 한글, 줌 변경, 캐시 재로딩, HDR 결과도 직접 확인한다.

드로우 콜 감소율 = (변경 전 평균 - 변경 후 평균) / 변경 전 평균 × 100.
제공된 174를 기준으로 쓸 수 있지만, 같은 장면/설정에서 측정했을 때만 유효하다.
여러 CSV 행의 평균을 합칠 때에는 frames를 가중치로 사용한다.

## 6. 다음 개선 판단 기준

- draws 감소 + upload_bytes/submit_ms 증가: CPU 혼합 배치 비용 조사, 정적 지오메트리 공유 경로 검토.
- ui_draws 비중 증가: 한글 텍스처 아틀라스와 UI 상태 전환 조사.
- post_draws는 일정하나 해상도에 따라 느림: GPU 시간 측정 후 후처리 해상도/패스 조정.
- cache_io_ms와 frame_ms_max 동반 증가: 캐시 비동기 읽기/쓰기, 선행 로딩 검토.
- 안정 구간에도 특정 키 build 반복: 키 안정성/캐시 무효화/스트리밍 수명 조사.
- sync_ms 비중 증가: Actor 전체 동기화 및 청크/구조물 순회 개선.
- 인스턴싱 비율이 낮음: 표시 순서를 보장할 수 있는 불투명 영역 분리 후 재정렬 검토.
- GPU 병목 여부 판단: 비동기 GPU timer query 추가. 측정 목적으로 매 프레임 glFinish를 넣지 않는다.

## 7. 변경 파일

- SceneBatch.cpp: 혼합 배치, 연속 GPU 인스턴싱, 정적 GPU 메시 지연 업로드
- SceneMesh.cpp: 메모리/파일 캐시 진입점과 메시 수명
- SceneMeshCache.cpp: 파일 포맷, 검증, 저장/복원
- SceneRenderer.h/.cpp: 배치 연결, 단위 삼각형/타원/ASCII 메시 재사용
- Shaders/Scene.vs: 인스턴스 변환·색·알파·발광 입력
- FrameProfiler.h: 구간 통계, CSV, 메시 수명 이벤트
- SimpleGame.cpp: 패스별 계측, 청크 캐시 서명, 로그/캐시 경로 초기화
- Visual Studio 프로젝트: 새 소스 파일 등록

셰이더 소스는 수정했으며 기존 빌드 후 복사 타깃을 사용한다. 실행 폴더의 셰이더 사본은 수동 변경하지 않았다.

확장 시 주의: DrawMesh는 즉시 GL 호출이 아닌 제출 예약이다. 외부 코드에서 프레임버퍼·블렌딩·프로그램 상태를 직접 바꾸거나 별도 GL draw를 끼워 넣기 전에 renderer.Flush()로 예약분을 제출해야 한다.
